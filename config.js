// Change mode to "http" only after connecting a server implementing BACKEND.md.
window.LK_CONFIG = Object.freeze({mode:'mock',baseUrl:'/api/v1',mockLatencyMs:100,requestTimeoutMs:10000,pollIntervalMs:800,maxPollDurationMs:30000});
