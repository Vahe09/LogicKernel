window.LKAdvanced.add('ts', [
['Структурная типизация и совместимость','TypeScript сравнивает структуру доступных членов, а не только имена объявленных типов, это позволяет подставлять независимые реализации одного контракта, лишние свойства и проверки литералов объектов имеют отдельные правила, типы исчезают при выполнении JavaScript, поэтому совместимость во время компиляции не доказывает правильность внешних данных',`type Named = { name: string };\nfunction greet(value: Named): string { return value.name; }\nconst user = { name: "Ann", age: 20 };\nconsole.log(greet(user));`,'опишите независимые реализации одного интерфейса и покажите границы их совместимости','совместимая структура принимается|отсутствующее поле даёт ошибку типов|внешнее значение проходит отдельную проверку','приведение as не преобразует и не проверяет данные','сравните структурный контракт и явную маркировку доменного значения'],
['Union типы и сужение','Объединение описывает несколько допустимых вариантов значения, проверка управляющего потока сужает тип внутри ветви, общий дискриминатор позволяет связать поля с конкретным состоянием, проверка truthy не всегда различает пустую строку и отсутствие, исчерпывающая обработка должна обнаруживать добавление нового варианта на этапе проверки типов',`type Result = { ok: true; value: number } | { ok: false; error: string };\nfunction show(result: Result): string {\n  return result.ok ? String(result.value) : result.error;\n}\nconsole.log(show({ ok: true, value: 3 }));`,'смоделируйте состояния загрузки данных без несовместимых сочетаний полей','данные доступны только в успешной ветви|ошибка имеет сообщение|новый вариант требует обработки','необязательные поля одного большого объекта допускают невозможное состояние','сравните дискриминируемое объединение и набор boolean флагов'],
['Generic функции и сохранение связи типов','Параметр типа выражает связь между входом и результатом, в отличие от any он сохраняет проверяемую информацию, ограничение generic перечисляет необходимые операции и не должно обещать больше, чем функция действительно использует, слишком много параметров типов усложняют интерфейс без новых гарантий, вывод типа полезнее обязательного ручного указания там, где связь однозначна',`function first<T>(items: readonly T[]): T | undefined {\n  return items[0];\n}\nconsole.log(first([1, 2]));`,'создайте типобезопасные функции выбора и группировки элементов с сохранением типа результата','пустой вход отражён в типе|исходный массив не изменяется|разные типы элементов не превращаются в any','обещание T для пустого массива скрывает undefined','сравните generic функцию и несколько конкретных перегрузок'],
['keyof и индексируемый доступ','keyof связывает ключи с конкретным объектным типом, индексируемый доступ получает тип значения по ключу, пара параметров T и K позволяет сохранить связь аргумента с результатом, произвольная строка не является доказанным ключом, проверка входного ключа во время выполнения нужна для внешних запросов',`function pick<T, K extends keyof T>(value: T, key: K): T[K] {\n  return value[key];\n}\nconsole.log(pick({ name: "Ann", age: 20 }, "age"));`,'реализуйте безопасный выбор поля и проекцию набора полей модели','неизвестный ключ отвергается|тип выбранного значения сохраняется|внешний ключ проверяется отдельно','обобщение ключа до string теряет связь с объектом','решите, нужна ли динамическая проекция или достаточно явного DTO'],
['Mapped типы и модификаторы','Mapped тип строит новый объектный тип обходом ключей другого, он позволяет выразить readonly, optional или изменение типа значений, такая операция описывает статический контракт и не копирует объект, глубокие преобразования должны учитывать массивы, функции и рекурсивные структуры, лаконичность объявления не должна скрывать назначение публичного типа',`type Flags<T> = { [K in keyof T]: boolean };\ntype User = { name: string; age: number };\nconst enabled: Flags<User> = { name: true, age: false };\nconsole.log(enabled);`,'постройте тип карты изменений формы и реализацию применения разрешённых изменений','набор ключей совпадает с моделью|неизменяемые поля исключены|типовая карта не заменяет проверку входного JSON','Readonly не обеспечивает глубокую неизменяемость во время выполнения','сравните вычисляемый тип и явное описание публичной команды'],
['Условные типы и infer','Условный тип выбирает результат по совместимости типов, infer извлекает часть структуры в подходящей ветви, распределение по объединению может быть полезным или неожиданным, типовая программа должна иметь понятный предел сложности, публичному пользователю библиотеки важны читаемые ошибки и предсказуемый вывод, а не максимальное количество вычислений компилятора',`type ElementOf<T> = T extends readonly (infer U)[] ? U : never;\ntype Value = ElementOf<readonly number[]>;\nconst value: Value = 5;\nconsole.log(value);`,'опишите тип извлечения результата операции и проверьте его на объединениях и неподходящих входах','массив даёт тип элемента|неподходящая структура даёт never|объединение имеет документированное поведение','распределительный условный тип может неожиданно обработать каждую ветвь отдельно','сравните выразительность типа со стоимостью его понимания и проверки'],
['unknown и граница валидации','unknown запрещает использовать значение до проверки и поэтому подходит для недоверенного входа, пользовательский type guard должен проверять именно то, что обещает возвращаемый предикат, утверждение типа обходится без такой проверки и переносит риск на разработчика, полезно возвращать отдельный Result с путём ошибки вместо неразличимого false',`type User = { id: number };\nfunction isUser(value: unknown): value is User {\n  return typeof value === "object" && value !== null && "id" in value && typeof value.id === "number";\n}\nconsole.log(isUser({ id: 1 }));`,'реализуйте разбор внешнего сообщения через unknown и возврат типизированного результата валидации','null отвергается|неверный тип поля отвергается|успех содержит нормализованное значение','неполный guard создаёт ложное доказательство для компилятора','сравните булев guard и подробный результат разбора'],
['Доменные значения и единицы измерения','Одинаковые примитивы могут обозначать разные сущности, идентификатор заказа нельзя случайно подменять идентификатором пользователя, брендированный тип ограничивает смешение на этапе компиляции, создавать его следует через проверяющую функцию, сериализация снимает такую гарантию и требует повторной проверки на границе, брендинг не является механизмом безопасности',`type UserId = string & { readonly __brand: "UserId" };\nfunction userId(raw: string): UserId {\n  if (!raw.startsWith("u_")) throw new Error("invalid id");\n  return raw as UserId;\n}\nconsole.log(userId("u_7"));`,'разделите идентификаторы и денежные единицы модели через проверяемые конструкторы','несовместимые доменные значения не смешиваются|неверное исходное значение отвергается|данные после JSON проходят проверку заново','массовое приведение as уничтожает смысл доменного типа','сравните бренды и отдельные классы значений'],
['Типизация асинхронных контрактов','Promise параметризуется типом успешного результата, тип исключения обычно не закодирован в Promise, поэтому для ожидаемого отказа полезен явный результат с дискриминатором, отмена и повтор относятся к контракту операции, типизированный API не избавляет от проверки серверного ответа, для клиента важно различать ошибку транспорта и отказ предметной области',`type Loaded<T> = { ok: true; data: T } | { ok: false; message: string };\nasync function load(): Promise<Loaded<number>> {\n  return { ok: true, data: 7 };\n}\nload().then(console.log);`,'создайте типизированный клиент через внедряемый транспорт с отдельными ветвями сетевого и бизнес отказа','успех содержит проверенные данные|ожидаемый отказ обрабатывается явно|отмена не маскируется как успешный ответ','Promise<T> не описывает автоматически все исключения','сравните исключения и Result для ожидаемых ошибок'],
['Variance и безопасные callback интерфейсы','Позиция типа в параметре и результате функции влияет на безопасную подстановку, обработчик должен принимать все значения, которые обещает вызывающий код, слишком узкий callback нарушает контракт даже при похожих объектах, strict проверка помогает выявить такие ошибки, изменяемые контейнеры требуют особой осторожности, публичные readonly представления сокращают возможности нежелательной записи',`type Handler<T> = (value: T) => void;\nfunction visit(items: readonly string[], handler: Handler<string>) {\n  for (const item of items) handler(item);\n}\nvisit(["a"], console.log);`,'спроектируйте API обработчиков событий с безопасными типами параметров и неизменяемыми входами','обработчик принимает весь обещанный набор значений|вход нельзя изменить через readonly API|ошибочная подстановка обнаруживается проверкой типов','узкая функция не может обслуживать произвольное значение более широкого типа','сравните удобство callback и явный интерфейс обработчика'],
['Типовые тесты и runtime тесты','Проверка компиляции доказывает соответствие статическому контракту, runtime тест проверяет наблюдаемое поведение JavaScript, один вид проверки не заменяет другой, отрицательные типовые примеры должны действительно ожидать ошибку, иначе расширение до any останется незамеченным, публичная библиотека нуждается в проверках сценариев использования, а не только внутренней реализации',`function double(value: number): number { return value * 2; }\nconst result: number = double(3);\n// @ts-expect-error string is not accepted\ndouble("3");\nconsole.log(result);`,'подготовьте для небольшой библиотеки положительные и отрицательные проверки типов и отдельные проверки поведения','ошибочный вызов не компилируется без ожидаемой ошибки|правильный вызов выводит нужный тип|runtime границы проверены отдельно','any может сделать все типовые проверки бессодержательными','выберите минимальный набор типовых примеров для публичного API'],
['Конфигурация компилятора и модули','Настройки проверки типов, разрешения модулей и генерации JavaScript решают разные задачи, strict выявляет неявные опасные допущения, выбранная среда исполнения должна понимать полученный формат модулей, path alias компилятора не всегда создаёт соответствующее разрешение во время выполнения, воспроизводимый проект фиксирует конфигурацию и проверяет результат в целевой среде',`export type Config = { readonly apiUrl: string };\nexport function endpoint(config: Config, path: string): string {\n  return new URL(path, config.apiUrl).href;\n}`,'подготовьте библиотечный модуль и опишите согласованную конфигурацию проверки, сборки и запуска','проверка типов запускается отдельно|импорты разрешаются в целевой среде|сборка воспроизводится по инструкции','успешный tsc не гарантирует, что runtime найдёт модуль по alias','сравните конфигурацию приложения и публикуемой библиотеки'],
['Контракты API и совместимость','Типы клиента должны отражать версию реально передаваемых данных, генерация типов из схемы уменьшает ручное дублирование, но не отменяет проверку входа и согласование изменений, необязательное поле и поле со значением null имеют разный смысл, смена enum может сломать исчерпывающую обработку старого клиента, полезны контрактные примеры обеих версий',`type V1 = { id: string; name: string };\ntype V2 = V1 & { displayName?: string };\nfunction label(user: V2): string { return user.displayName ?? user.name; }\nconsole.log(label({ id: "1", name: "Ann" }));`,'спроектируйте совместимое расширение DTO и валидатор двух поддерживаемых версий','старый ответ остаётся читаемым|null не путается с отсутствием|неизвестная версия обрабатывается явно','изменение смысла старого поля незаметно ломает клиентов','сравните отдельные версии DTO и аддитивное расширение'],
['Типобезопасный автомат процесса','Состояние процесса полезно моделировать как закрытый набор вариантов с допустимыми действиями, типы могут связать доступность полей с состоянием и сократить число невозможных сочетаний, но реальный внешний порядок событий всё равно проверяется во время выполнения, редуктор должен иметь определённое поведение для устаревшего или повторного события',`type State = { kind: "idle" } | { kind: "ready"; value: number };\ntype Event = { kind: "loaded"; value: number } | { kind: "reset" };\nfunction reduce(state: State, event: Event): State {\n  return event.kind === "reset" ? { kind: "idle" } : { kind: "ready", value: event.value };\n}`,'смоделируйте процесс отправки задания со стадиями, версиями и защитой от повторного события','результат связан с текущей попыткой|повтор не удваивает эффект|недопустимый переход обнаруживается','статический тип не контролирует последовательность сетевых событий','сравните явный автомат и независимые поля состояния'],
['Границы пакетов и стоимость типов','Большой проект требует устойчивых публичных границ пакетов, внутренние типы не должны случайно становиться обязательными для потребителя, сложные рекурсивные типы могут увеличивать время проверки и ухудшать диагностику, архитектурный выигрыш измеряется стоимостью изменений и ясностью зависимостей, иногда явный простой тип лучше универсального вычисления',`export interface UserReader {\n  find(id: string): Promise<{ id: string; name: string } | null>;\n}\nexport async function nameOf(reader: UserReader, id: string) {\n  return (await reader.find(id))?.name ?? null;\n}`,'выделите границу пакета домена и адаптера, подготовьте проверку обратной совместимости публичных типов','домен не импортирует конкретный транспорт|внутренняя замена не меняет клиента|диагностика типов остаётся читаемой','экспорт всех внутренних типов превращает устройство пакета в публичный контракт','сравните универсальный типовой конструктор и конкретный интерфейс'],
['Типизированная платформа обработки команд','В итоговом проекте типы связывают команды, результаты и состояние, runtime валидаторы охраняют внешние границы, архитектура должна оставлять возможность заменить транспорт и хранилище, доказательство качества включает типовые проверки, сценарии отказа, версионирование и понятную документацию, отсутствие ошибок компилятора не означает отсутствия бизнес дефектов',`type Command = { kind: "create"; id: string };\ntype Reply = { ok: true; id: string } | { ok: false; reason: string };\nfunction handle(command: Command, ids: Set<string>): Reply {\n  if (ids.has(command.id)) return { ok: false, reason: "duplicate" };\n  ids.add(command.id);\n  return { ok: true, id: command.id };\n}`,'соберите библиотеку команд с валидацией unknown, типизированными результатами, внедряемыми адаптерами и совместимым обновлением DTO','ошибочный ввод не попадает в домен|повторная команда следует контракту|замена адаптера не меняет бизнес типы','типовой каркас без runtime проверки принимает недоверенные данные','обоснуйте границы пакетов и степень обобщения публичного API']
]);
