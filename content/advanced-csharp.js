window.LKAdvanced.add('csharp', [
['Типы значений ссылки и nullable','Тип значения копируется как значение, ссылочный тип передаёт ссылку на объект, это различие влияет на наблюдаемое изменение данных, nullable аннотации помогают статической проверке возможного null, но не заменяют проверку внешнего входа, значения модели должны иметь определённое состояние после создания, копирование ссылки не создаёт независимую копию объекта',`string? name = null;\nstring label = name ?? "guest";\nint first = 3;\nint second = first;\nsecond++;\nConsole.WriteLine($"{label} {first} {second}");`,'спроектируйте модель профиля с явной обработкой отсутствующих данных и независимым снимком для чтения','null обрабатывается явно|снимок не меняется вместе с исходником|значения по умолчанию осмысленны','null подавляется оператором ! без реальной проверки','сравните изменяемый класс и значение для публичного снимка'],
['Интерфейсы и композиция','Интерфейс описывает необходимую возможность, композиция связывает объект с реализацией этой возможности, зависимость от небольшого контракта облегчает проверку и замену адаптера, наследование требует сохранения поведения базового типа, слишком широкий интерфейс заставляет клиентов зависеть от ненужных операций',`var service = new Greeting(new FixedClock());\nConsole.WriteLine(service.Get());\ninterface IClock { int Hour { get; } }\nsealed class FixedClock : IClock { public int Hour => 12; }\nsealed class Greeting(IClock clock) { public string Get() => clock.Hour < 18 ? "day" : "evening"; }`,'создайте сервис с внедряемыми часами и хранилищем через узкие интерфейсы','ядро проверяется без внешних служб|реализации взаимозаменяемы|время контролируется тестом','сервис локатор скрывает реальные зависимости компонента','сравните конструкторную инъекцию и создание зависимостей внутри метода'],
['Коллекции и generic контракты','Generic коллекция сохраняет тип элемента и уменьшает необходимость небезопасных приведений, выбор между списком, словарём и множеством определяется операциями, IReadOnlyList ограничивает доступ через интерфейс, но не гарантирует неизменяемость лежащего внутри объекта, при передаче коллекции наружу нужно определить, видит ли клиент живое состояние или снимок',`var counts = new Dictionary<string, int>();\nforeach (var word in new[] { "api", "sql", "api" })\n    counts[word] = counts.GetValueOrDefault(word) + 1;\nConsole.WriteLine(counts["api"]);`,'постройте индекс частот с явными правилами сравнения строк и публичным снимком результата','повторы учитываются|правило регистра стабильно|клиент не изменяет внутренний индекс','readonly интерфейс не замораживает исходную коллекцию','сравните выдачу снимка и живого представления'],
['LINQ и отложенное выполнение','Многие LINQ операции описывают отложенный обход, фактическое вычисление начинается при перечислении, повторное перечисление может повторить работу и увидеть новое состояние источника, ToList материализует снимок с ценой памяти, предикат с побочным эффектом усложняет рассуждение, важно отличать IEnumerable в памяти и запрос, который переводит внешний провайдер',`var values = new List<int> { 1, 2, 3 };\nvar query = values.Where(x => x > 1);\nvalues.Add(4);\nConsole.WriteLine(string.Join(",", query));`,'реализуйте отчёт с фильтрацией, группировкой и явно выбранным моментом материализации','повторный обход имеет описанное поведение|пустой источник корректен|дорогая операция не повторяется незаметно','ленивый запрос может увидеть изменения после своего создания','сравните отложенный запрос и неизменяемый снимок отчёта'],
['Records и доменные инварианты','Record удобен для представления данных со сравнением значений, но сам синтаксис не доказывает корректность полей, доменное значение должно создаваться через проверяемую границу, with создаёт копию с изменением полей и требует внимания к вложенным ссылкам, денежные и идентификационные значения полезно отделять от произвольных примитивов',`var price = new Money(150);\nConsole.WriteLine(price.Cents);\nsealed record Money {\n    public int Cents { get; }\n    public Money(int cents) {\n        if (cents < 0) throw new ArgumentOutOfRangeException(nameof(cents));\n        Cents = cents;\n    }\n}`,'создайте модель заказа с проверяемыми денежными значениями и разрешёнными переходами','отрицательная сумма отвергается|переход оплаты определён|сравнение значений соответствует предметному смыслу','публичный сеттер позволяет обходить инвариант конструктора','сравните record значение и изменяемую сущность'],
['Исключения и освобождение ресурсов','IDisposable задаёт протокол освобождения ресурса, using привязывает его к области жизни, исключение не должно оставлять ресурс в неопределённом состоянии, типы ошибок следует разделять по возможности восстановления, повторное выбрасывание должно сохранять диагностическую информацию, освобождение ресурса не является автоматическим откатом бизнес операции',`using var stream = new MemoryStream();\nstream.WriteByte(42);\nstream.Position = 0;\nConsole.WriteLine(stream.ReadByte());`,'реализуйте обработчик ресурса с гарантированным освобождением и классификацией ошибок','успех освобождает ресурс|ошибка сохраняет причину|частичная операция имеет явное восстановление','поглощённое исключение выдаёт повреждённый результат за успешный','определите границу владения ресурсом и границу бизнес отката'],
['Тестируемые сервисы и детерминизм','Независимая проверка управляет исходными данными и наблюдает контракт, внедрение часов, генератора идентификаторов и хранилища делает результат воспроизводимым, чрезмерная проверка внутренней последовательности вызовов связывает тест с реализацией, интеграционные проверки нужны там, где важны реальные правила внешнего компонента',`static bool Expired(DateTimeOffset deadline, Func<DateTimeOffset> now) => now() >= deadline;\nvar instant = DateTimeOffset.Parse("2026-01-01T00:00:00Z");\nConsole.WriteLine(Expired(instant, () => instant));`,'подготовьте сервис срока действия токена с контролируемыми часами и набором границ','сам момент истечения обработан|часовой пояс не меняет сравнение момента|тест не ждёт реального времени','DateTime.Now внутри логики делает тест зависимым от окружения','разделите проверку доменного времени и интеграцию с реальным хранилищем'],
['Асинхронные операции и CancellationToken','Task представляет асинхронную операцию, await не блокирует текущий поток ожиданием незавершённой задачи, токен отмены передаётся по всей цепочке, отмена является согласованным запросом и не откатывает внешнюю транзакцию автоматически, блокирующее ожидание Result усложняет поведение и может создавать взаимные ожидания в некоторых контекстах',`using var source = new CancellationTokenSource();\nawait Task.Delay(1, source.Token);\nConsole.WriteLine("done");`,'создайте асинхронный сервис из нескольких зависимых шагов с передачей токена отмены','отмена прекращает дальнейшие шаги|ресурс освобождается|ошибка не превращается в отмену','токен передан только первому вызову и теряется глубже','сравните общий дедлайн запроса и отдельные таймауты шагов'],
['Конкурентность и ограничение нагрузки','Совместное ожидание задач не ограничивает число одновременно начатых операций, слишком большая конкурентность перегружает память и внешнюю службу, SemaphoreSlim позволяет задавать ограничение, освобождение разрешения должно находиться в finally, разделяемые коллекции требуют согласованной стратегии доступа, порядок завершения не равен порядку исходного ввода',`using var gate = new SemaphoreSlim(2);\nasync Task<int> Work(int value) {\n    await gate.WaitAsync();\n    try { await Task.Delay(1); return value * 2; }\n    finally { gate.Release(); }\n}\nConsole.WriteLine(string.Join(",", await Task.WhenAll(new[] {1,2,3}.Select(Work))));`,'реализуйте ограниченную параллельную обработку набора заданий с устойчивым порядком результата','лимит одновременных операций соблюдается|разрешение возвращается при ошибке|порядок результата определён','создание всех задач сразу может сохранить чрезмерный объём состояния','сравните семафор и ограниченную очередь работников'],
['HTTP границы и внедряемые адаптеры','Сетевой клиент должен различать отсутствие связи, статус сервера и ошибку формата, общий бюджет запроса включает ожидания и повторы, долгоживущий транспорт требует корректного управления соединениями, бизнес логика не должна зависеть от конкретной реализации HTTP, повтор изменяющей команды допустим только при защищённом протоколе',`static string ReadName(int status, string? name) {\n    if (status != 200) throw new InvalidOperationException("upstream failure");\n    return name ?? throw new FormatException("name missing");\n}\nConsole.WriteLine(ReadName(200, "Ann"));`,'спроектируйте адаптер внешнего каталога с дедлайном, валидацией и проверяемой политикой повторов','неверное тело классифицируется|таймаут ограничивает всю операцию|небезопасная команда не повторяется автоматически','каждый слой повторяет запрос и перемножает число попыток','сравните повтор в транспортном слое и на уровне бизнес операции'],
['SQL доступ и единица работы','Транзакция защищает инвариант нескольких изменений, граница единицы работы должна соответствовать бизнес операции, параметризованные запросы отделяют данные от SQL, оптимистическая конкуренция обнаруживает изменение записи между чтением и записью, имитация транзакции в словаре не доказывает поведение реальной базы',`static bool TryChange(ref int version, int expected) {\n    if (version != expected) return false;\n    version++;\n    return true;\n}\nint revision = 2;\nConsole.WriteLine(TryChange(ref revision, 2));`,'постройте контракт репозитория с версией записи и планом атомарного изменения связанных данных','устаревшая версия даёт конфликт|частичный отказ откатывается|повтор не теряет изменение другого пользователя','чтение и запись без проверки версии создают потерянное обновление','сравните оптимистическую проверку версии и блокировку записи'],
['Сериализация и версии DTO','DTO описывает формат внешних данных и не обязан совпадать с доменной моделью, отсутствие поля и null могут иметь разный смысл, сериализация требует явных правил имён, дат и чисел, добавление нового варианта состояния влияет на старых клиентов, проверка совместимости должна использовать реальные JSON примеры',`using System.Text.Json;\nvar json = JsonSerializer.Serialize(new { Id = 7, Name = "Ann" });\nConsole.WriteLine(json);`,'подготовьте версионируемый контракт команды и отдельное преобразование в доменную модель','неизвестная версия обработана|недостающие поля проверяются|старый клиент сохраняет совместимое поведение','прямая сериализация сущности раскрывает внутренние поля','сравните отдельные DTO версии и аддитивное расширение'],
['Память Span и измерение выделений','Оптимизация памяти требует измерять выделения на конкретном пути, Span представляет доступ к непрерывной области без обязательного создания нового массива, срок жизни и ограничения такого представления важны для безопасности, уменьшение аллокаций не всегда даёт заметное ускорение, читаемость и стоимость сопровождения входят в решение',`ReadOnlySpan<char> text = "12345".AsSpan();\nConsole.WriteLine(int.Parse(text[..3]));`,'исследуйте разбор большого набора записей и сравните копирование подстрок с представлениями','результат разбора одинаков|границы диапазона проверены|замер фиксирует объём входа и выделений','оптимизация до профилирования усложняет незначимый путь','сравните выигрыш памяти и сложность API со временем жизни представления'],
['События диагностики и эксплуатация','Наблюдаемость должна связывать запрос, зависимые операции и итоговый результат, структурированные поля делают события пригодными для поиска, пользовательские секреты нельзя переносить в логи, метрики требуют ограниченной кардинальности, корректный сигнал позволяет проверить гипотезу о конкретном отказе, а не просто увеличить количество сообщений',`var operationId = Guid.NewGuid().ToString("N");\nConsole.WriteLine(System.Text.Json.JsonSerializer.Serialize(new { Event = "order_started", OperationId = operationId }));`,'спроектируйте события и метрики сервиса с корреляцией и сценарием диагностики отказа','операция прослеживается целиком|секретные данные исключены|рост ошибок различим по причине','идентификатор каждого пользователя в метках метрики создаёт огромную кардинальность','выберите сигналы под конкретные эксплуатационные вопросы'],
['Фоновые обработчики и надёжные повторы','Фоновая обработка отделяет принятие команды от длительной работы, сообщение может быть доставлено повторно, поэтому бизнес эффект защищается ключом и устойчивой записью результата, согласованная запись outbox помогает связать изменение данных с будущим событием, завершение процесса должно либо закончить обработку, либо оставить возможность безопасного повтора',`var processed = new Dictionary<string, int>();\nint Apply(string key, int value) {\n    if (processed.TryGetValue(key, out var old)) return old;\n    return processed[key] = value * 2;\n}\nConsole.WriteLine(Apply("a", 3));`,'спроектируйте обработчик очереди с идемпотентностью, ограниченными повторами и сохранением причины отказа','повтор не удваивает эффект|ядовитое сообщение не блокирует очередь|остановка позволяет безопасное продолжение','память одного процесса не защищает повтор после перезапуска','сравните атомарную запись бизнес эффекта и отдельную доставку события'],
['Сервис заказов на C#','Итоговая система связывает доменную модель, репозиторий, асинхронные адаптеры и наблюдаемость через ясные контракты, архитектура должна выдерживать замену инфраструктуры и рост требований, важны проверяемые инварианты, стратегия совместимых изменений и понимание эксплуатационных отказов, количество проектов в solution само по себе не определяет качество разделения',`static string CreateOrder(int amount, Func<int, string> save) {\n    if (amount <= 0) throw new ArgumentOutOfRangeException(nameof(amount));\n    return save(amount);\n}\nConsole.WriteLine(CreateOrder(100, value => $"order-{value}"));`,'соберите сервис заказов с доменными значениями, транзакционным контрактом, асинхронной обработкой и планом совместимого выпуска','бизнес ядро проверяется отдельно|ошибка зависимости не выдаёт успех|повтор команды безопасен','избыточные слои скрывают реальные границы вместо их защиты','обоснуйте модульную архитектуру через сценарии изменений и отказов']
]);
