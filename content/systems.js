(() => {
const {specs, L} = window.LKCurriculum;
const c = body => '#include <stdio.h>\n\nint main(void)\n{\n'+body.split('\n').map(s=>'    '+s).join('\n')+'\n    return 0;\n}';
const cpp = (body,headers='') => '#include <iostream>\n'+headers+'\nint main()\n{\n'+body.split('\n').map(s=>'    '+s).join('\n')+'\n    return 0;\n}';
const go = body => 'package main\n\nimport "fmt"\n\nfunc main() {\n'+body.split('\n').map(s=>'    '+s).join('\n')+'\n}';
const rust = body => 'fn main() {\n'+body.split('\n').map(s=>'    '+s).join('\n')+'\n}';
specs.c = [
L('Программа на C: от текста к запуску','Программа на C — исходный текст, который компилятор переводит в исполняемый код. Не нужно понимать весь компьютер, чтобы начать. Сначала научимся узнавать обязательные части маленькой программы и менять одно сообщение.\nstdio.h содержит объявления стандартных функций ввода и вывода. main — место начала выполнения. void в скобках означает отсутствие параметров. printf получает строку, а последовательность \\n внутри строки обозначает переход на новую строку.',c('printf("Hello, C!\\n");'),'Подключение stdio.h позволяет компилятору узнать printf.|Фигурные скобки main ограничивают тело программы.|printf выводит сообщение; return 0 сообщает об успешном завершении.','Не записывайте /n вместо \\n: нужен обратный слеш.','Выведите Hello, LogicKernel! на отдельной строке.','Hello, LogicKernel!','Сохраните оболочку main и замените текст в printf.'),
L('Скобки, инструкции и комментарии','Синтаксис задаёт форму допустимого кода. В C каждая обычная инструкция заканчивается ;. Фигурные скобки объединяют инструкции, а круглые используются для параметров и аргументов. Отступы помогают читать, но не заменяют скобки.\nКомментарий // продолжается до конца строки. Компилятор игнорирует его содержимое. Если программа не собирается, сначала проверьте пары кавычек и скобок, затем ; в конце предыдущей инструкции.',c('// Показываем этапы\nprintf("Start\\n");\nprintf("Done\\n");'),'Комментарий объясняет цель двух сообщений.|Первый printf выполняется раньше второго.|Каждый \\n завершает отдельную строку вывода.','Не добавляйте ; после #include — это директива, а не обычная инструкция.','Добавьте комментарий и выведите One, затем Two на отдельных строках.','One\nTwo','Внутри main нужны два printf.'),
L('Тип int и первое присваивание','Переменная — именованное место для значения. int означает целочисленный тип. В int score = 10 создаётся переменная и сразу получает начальное значение. Инициализация особенно важна в C: локальная переменная без значения не становится автоматически нулём.\nДля вывода int в printf используется спецификатор %d. Он служит местом для числа из следующего аргумента. Количество и тип аргументов должны соответствовать шаблону.',c('int score = 10;\nscore = score + 5;\nprintf("%d\\n", score);'),'Сначала score равен 10.|Правая часть читает прежнее значение и прибавляет 5.|%d заменяется числовым представлением score.','Не читайте неинициализированную переменную и не печатайте int через %s.','Создайте int points = 20, прибавьте 30 и выведите число.','50','В printf передайте шаблон "%d\\n" и points.'),
L('Арифметика и дробные числа','Операции зависят от типов. Деление целых 7 / 2 даёт 3: дробная часть отбрасывается. Если хотя бы один операнд имеет тип double, результат может содержать дробь.\nprintf использует %f для double. Запись %.1f оставляет один знак после десятичной точки в выводе. Форматирование не меняет само значение, только его представление.',c('int whole = 7 / 2;\ndouble fraction = 7.0 / 2;\nprintf("%d\\n", whole);\nprintf("%.1f\\n", fraction);'),'7 и 2 — целые, поэтому whole получает 3.|7.0 задаёт дробный тип и приводит к 3.5.|Спецификатор определяет, как printf читает аргумент.','Не делите на ноль и не рассчитывайте, что int сохранит дробь.','Посчитайте стоимость 6 предметов по 40 и выведите целое число.','240','Используйте int и умножение *.'),
L('Символы и строки','char хранит один символьный код. Одинарные кавычки задают символьную константу, двойные — строковый литерал. В C строка представляется массивом char с завершающим нулевым байтом.\nЗапись char name[] = "Ada" создаёт массив достаточного размера, включая завершающий ноль. Для целой строки printf использует %s, для одного символа — %c. Пока не меняйте длину строки и не записывайте за границы массива.',c('char letter = \'A\';\nchar name[] = "Ada";\nprintf("%c\\n", letter);\nprintf("%s\\n", name);'),'letter обозначает один символ.|name включает A, d, a и завершающий нулевой байт.|Формат %s читает символы до этого нуля.','Не пытайтесь присвоить длинную строку переменной char.','Создайте char language[] = "C" и выведите Language: C.','Language: C','Используйте printf("Language: %s\\n", language).'),
L('Условия и сравнения','if выбирает блок по результату условия. В C ноль считается ложью, ненулевое значение — истиной. Сравнение score >= 70 даёт результат, пригодный для такой проверки.\nФигурные скобки показывают границы ветвей. Используйте их даже для одной команды. Проверка равенства записывается ==, а = изменяет переменную и поэтому может создать трудно заметную ошибку.',c('int score = 80;\nif (score >= 70) {\n    printf("Pass\\n");\n} else {\n    printf("Try again\\n");\n}'),'Сравнение выполняется до выбора ветви.|80 >= 70 истинно, поэтому выводится Pass.|Второй блок пропускается.','if (score = 70) присваивает 70 вместо сравнения.','При age = 15 выведите Adult для age >= 18, иначе Young.','Young','Нужны две ветви if/else.'),
L('Массивы и допустимые индексы','Массив хранит элементы одного типа подряд. int numbers[3] выделяет место для трёх int. Допустимые индексы — 0, 1, 2. Индекс 3 уже находится за границей, даже если физически программа не упала.\nC обычно не проверяет границы при каждом обращении. Выход за пределы даёт неопределённое поведение: нельзя полагаться ни на результат, ни на отсутствие ошибки. Размер массива нужно учитывать явно.',c('int numbers[3] = {10, 20, 30};\nprintf("%d\\n", numbers[0]);\nprintf("%d\\n", numbers[2]);'),'Объявление задаёт размер и начальные значения.|Индекс 0 выбирает 10.|Индекс 2 выбирает последний элемент 30.','Не используйте numbers[3] для массива длины 3.','Создайте массив из 4 чисел {2, 4, 6, 8} и выведите первый и последний элементы.','2\n8','Последний индекс равен 3.'),
L('Цикл for и счётчик','Цикл for объединяет начальное значение счётчика, условие продолжения и шаг изменения. В for (int i = 0; i < 3; i++) сначала создаётся i, затем перед каждой итерацией проверяется условие.\nПосле тела выполняется i++, увеличивающее счётчик на 1. Когда i становится 3, условие i < 3 ложно, и цикл прекращается. Это удобно для безопасного прохода массива известной длины.',c('int numbers[3] = {2, 4, 6};\nint total = 0;\nfor (int i = 0; i < 3; i++) {\n    total += numbers[i];\n}\nprintf("%d\\n", total);'),'Счётчик принимает 0, 1, 2.|Каждый индекс находится внутри массива.|total после трёх шагов равен 12.','Условие i <= 3 добавит недопустимый индекс 3.','Сложите {5, 10, 15} циклом и выведите сумму.','30','Сумму объявите до цикла.'),
L('Функции и передача аргументов','Функция объединяет вычисление с именем. Перед именем стоит тип результата, внутри скобок — типы и имена параметров. Определим функцию до main, чтобы компилятор уже знал её при вызове.\nПараметр int получает копию переданного значения. Изменение параметра внутри функции не изменит исходную переменную вызывающего кода. Для результата используйте return.','#include <stdio.h>\n\nint square(int number) {\n    return number * number;\n}\n\nint main(void) {\n    printf("%d\\n", square(4));\n    return 0;\n}','square принимает int и возвращает int.|При вызове number получает 4.|return отдаёт 16 для печати.','Не размещайте обычное определение функции внутри main: стандартный C этого не допускает.','Напишите double_number(int number), возвращающую удвоенное число. Выведите результат для 9.','18','Определите функцию перед main.'),
L('Адреса и указатели без спешки','У каждой переменной есть место в памяти. Оператор & получает её адрес. Указатель — переменная, в которой хранится адрес объекта соответствующего типа. int *pointer означает указатель на int.\nВ выражении *pointer оператор разыменования обращается к значению по адресу. В объявлении * является частью записи типа указателя. Используем только адрес существующей локальной переменной, время жизни которой ещё не закончилось.',c('int score = 10;\nint *pointer = &score;\n*pointer = 25;\nprintf("%d\\n", score);'),'&score получает адрес score.|pointer хранит этот адрес.|Присваивание через *pointer меняет сам score на 25.','Нельзя разыменовывать неинициализированный указатель или NULL.','Создайте int value = 5, указатель на него, затем через указатель запишите 12 и выведите value.','12','Используйте & при получении адреса и * при обращении к значению.'),
L('Структуры: объединяем свойства','struct описывает набор полей, относящихся к одной сущности. Например, у результата есть балл и номер попытки. Структура позволяет передавать эти данные вместе, не теряя их смысл.\nОписание struct объявляет тип, но не создаёт конкретную запись. После него можно объявить переменную этого типа. Точка обращается к полю объекта структуры.','#include <stdio.h>\n\nstruct Result {\n    int score;\n    int attempt;\n};\n\nint main(void) {\n    struct Result result = {80, 1};\n    printf("%d\\n", result.score);\n    return 0;\n}','Описание содержит два поля типа int.|После закрытия определения структуры нужен ;.|Инициализация задаёт значения в порядке полей.','Не забывайте слово struct при объявлении переменной в этой форме C.','Создайте struct Product с полями price и quantity. Для {50, 3} выведите их произведение.','150','Читайте product.price и product.quantity.'),
L('Проект: безопасная сумма массива','Функция, получающая массив, должна отдельно знать, сколько элементов можно прочитать. При передаче в параметр int values[] размер исходного массива автоматически не передаётся. Поэтому добавим параметр count.\nДля маленького проекта достаточно суммы небольших чисел. Проверяйте count и границы. Обработку огромных чисел и переполнений можно добавить позже, но нельзя игнорировать допустимый диапазон чтения.','#include <stdio.h>\n\nint total(const int values[], int count) {\n    int result = 0;\n    for (int i = 0; i < count; i++) result += values[i];\n    return result;\n}\n\nint main(void) {\n    int values[3] = {10, 20, 30};\n    printf("Total: %d\\n", total(values, 3));\n    return 0;\n}','const запрещает функции изменять элементы через этот параметр.|count задаёт разрешённое число элементов.|При count = 0 цикл не выполняется и сумма остаётся нулём.','Не вычисляйте длину параметра массива через sizeof: внутри функции он уже не исходный массив.','Напишите total и выведите Total: 24 для {4, 8, 12}.','Total: 24','Передайте массив и его длину 3 отдельно.')
];
specs.cpp = [
L('Что такое C++ и точка входа main','Исходный код C++ хранится в текстовом файле. Компилятор проверяет его и создаёт исполняемую программу. main — функция, с которой начинается выполнение. На первом уроке не нужны классы или ручное управление памятью.\nЗаголовок iostream предоставляет поток вывода std::cout. std — пространство имён стандартной библиотеки, :: уточняет расположение имени. Оператор << отправляет очередное значение в поток.',cpp('std::cout << "Hello, C++!\\n";'),'iostream подключает объявления потока.|main содержит инструкции запуска.|Текст заканчивается \\n — переходом на новую строку.','Не заменяйте << на <, это разные операторы.','Выведите Hello, LogicKernel! одной строкой.','Hello, LogicKernel!','Оставьте оболочку примера и замените сообщение.'),
L('Читаем структуру программы','Код проще читать, когда логические части отделены пустыми строками. Директивы #include находятся сверху, затем определения функций. Внутри тела функции инструкции оформляйте отступами.\nКомментарий // объясняет намерение. Обычные инструкции заканчиваются ;. Сообщение компилятора часто указывает строку после пропущенной скобки или ;, поэтому проверяйте соседний участок.',cpp('// Выводим два этапа\nstd::cout << "Start\\n";\nstd::cout << "Finish\\n";'),'Комментарий не является выводом.|Оба выражения отправляют текст в один поток.|Порядок инструкций сохраняется.','Не ставьте ; после строки #include.','Добавьте комментарий и выведите First и Second на отдельных строках.','First\nSecond','Используйте два выражения std::cout.'),
L('Типы и инициализация','Тип задаёт вид допустимых значений. int хранит целые числа, double — числа с дробной частью, bool — true или false. Инициализация сразу даёт переменной начальное значение.\nЗапись int score = 10 понятна на старте. Позже встретится и int score{10}. Для переменной, которая не должна меняться, добавьте const. Название выбирайте по смыслу данных.',cpp('const int bonus = 5;\nint score = 10;\nscore = score + bonus;\nstd::cout << score << "\\n";'),'bonus нельзя переприсвоить.|score можно изменить, поэтому прибавление сохраняется в нём.|Вывод получает число 15.','Не читайте локальные числовые переменные до инициализации.','Создайте int count = 3, увеличьте на 2 и выведите.','5','Повторное присваивание не требует повторять int.'),
L('Арифметика и преобразование типов','Операторы +, -, * и / выполняют арифметические действия. Деление int на int даёт целочисленный результат. Для дробного деления преобразуйте один из операндов в double до операции.\nstatic_cast<double> явно показывает намерение изменить тип. Это не изменение исходной переменной: преобразование создаёт значение нужного типа для выражения.',cpp('int total = 7;\nint count = 2;\ndouble average = static_cast<double>(total) / count;\nstd::cout << average << "\\n";'),'total и count остаются int.|Преобразование происходит перед делением.|Получается 3.5, а не 3.','static_cast<double>(total / count) преобразует уже усечённый результат.','Вычислите произведение 35 и 4 и выведите его.','140','Для целой стоимости достаточно int.'),
L('Строки std::string','std::string — стандартный тип для текста. Он управляет хранением символов и поддерживает операции вроде соединения строк. Для использования подключите <string>.\nСтроковый литерал в кавычках может инициализировать std::string. Оператор + соединяет строковые значения. Числа автоматически не превращаются в строки для любого сложения: для вывода нескольких значений удобнее цепочка <<.',cpp('std::string name = "Ada";\nstd::cout << "Hello, " << name << "!\\n";','#include <string>\n'),'Подключается дополнительный заголовок.|name получает текст Ada.|Каждый << добавляет часть сообщения к потоку.','Не путайте char, хранящий один символ, и std::string, хранящий текст.','Создайте std::string language = "C++" и выведите Learning C++.','Learning C++','Соедините постоянный текст и переменную через <<.'),
L('Ветвление if/else','Сравнение возвращает логический результат. if выбирает одну из ветвей. Условие находится в круглых скобках, а действия — в фигурных.\nОператор == сравнивает значения, = присваивает. Привычка читать условие вслух помогает не перепутать их: «score больше или равен 70» — это проверка, а не изменение score.',cpp('int score = 80;\nif (score >= 70) {\n    std::cout << "Pass\\n";\n} else {\n    std::cout << "Try again\\n";\n}'),'Сначала вычисляется сравнение.|При 80 выбирается Pass.|Остальные действия выполнятся после выбранной ветви.','Не пишите отдельный ; сразу после if (...): он создаёт пустую инструкцию.','При temperature = -2 выведите Cold, если значение ниже нуля, иначе Warm.','Cold','Сравните temperature < 0.'),
L('vector: коллекция изменяемой длины','std::vector хранит последовательность элементов одного типа. В vector<int> угловые скобки указывают тип элемента. push_back добавляет значение в конец, size сообщает количество.\nИндекс начинается с 0. Метод at проверяет границы и сообщает об ошибке, если индекс недопустим. Оператор [] не делает такую проверку автоматически, поэтому на старте используем at для явного доступа.',cpp('std::vector<int> scores = {10, 20};\nscores.push_back(30);\nstd::cout << scores.at(0) << "\\n";\nstd::cout << scores.size() << "\\n";','#include <vector>\n'),'Создаётся vector из двух чисел.|После push_back длина становится 3.|at(0) возвращает первый элемент 10.','Индекс size() уже за пределами коллекции.','Создайте vector<int> {5, 10}, добавьте 15 и выведите size().','3','Не забудьте #include <vector>.'),
L('Цикл по коллекции','Range-based for проходит по значениям коллекции без ручного счётчика. В for (int number : numbers) number на каждом шаге получает очередное число. Это удобный вариант, когда индекс не нужен.\nСумма создаётся до цикла. Если коллекция пуста, тело не выполнится ни разу и сумма сохранит начальное значение 0.',cpp('std::vector<int> numbers = {2, 4, 6};\nint total = 0;\nfor (int number : numbers) {\n    total += number;\n}\nstd::cout << total << "\\n";','#include <vector>\n'),'После первого шага total = 2.|После второго total = 6.|После третьего total = 12.','В range-based for между переменной и коллекцией стоит :, а не ;.','Сложите vector {3, 6, 9} через range-based for.','18','Печатайте сумму после закрывающей скобки цикла.'),
L('Функции и возвращаемый результат','Функция получает параметры и возвращает результат указанного типа. Определим её перед main, чтобы место вызова уже знало её сигнатуру. Сигнатура показывает имя и параметры функции.\nint перед именем говорит, что результат — целое число. return завершает текущий вызов. Один и тот же код можно вызвать несколько раз с разными аргументами.','#include <iostream>\n\nint square(int number) {\n    return number * number;\n}\n\nint main() {\n    std::cout << square(5) << "\\n";\n    return 0;\n}','Параметр number получает копию аргумента.|Вычисляется произведение.|main получает число 25 и выводит его.','Не забывайте возвращать значение на всех необходимых путях функции.','Напишите triple(int number) и выведите triple(4).','12','Функция возвращает number * 3.'),
L('Ссылки: второе имя существующего объекта','Ссылка — другое имя для уже существующего объекта. В int& alias = score символ & входит в объявление ссылки. Изменение alias изменяет score, потому что это не независимая копия.\nСсылка должна быть инициализирована. Для чтения больших объектов без копирования часто используют const-ссылку. В этом уроке сравним только обычную копию и ссылку на int.',cpp('int score = 10;\nint copy = score;\nint& alias = score;\nalias = 25;\nstd::cout << score << "\\n";\nstd::cout << copy << "\\n";'),'copy получает отдельное значение 10.|alias связан с исходным score.|Изменение через alias даёт score = 25, но copy остаётся 10.','Нельзя создавать ссылку на локальный объект, который уже прекратил существовать.','Создайте value = 7 и ссылку на него. Через ссылку присвойте 11 и выведите value.','11','Объявление ссылки: int& alias = value.'),
L('Структуры и объекты','struct объединяет связанные поля в тип. Например, Product содержит цену и количество. Конкретная переменная этого типа — объект. Через точку можно читать его поля.\nВ C++ поля struct по умолчанию доступны снаружи. Это удобно для простых записей. Классы с закрытыми данными и методами — следующий шаг после понимания такой структуры.','#include <iostream>\n\nstruct Product {\n    int price;\n    int quantity;\n};\n\nint main() {\n    Product item{50, 3};\n    std::cout << item.price * item.quantity << "\\n";\n    return 0;\n}','Определение структуры завершается ;.|Инициализация задаёт поля по порядку.|Произведение полей даёт 150.','Не забывайте ; после определения struct.','Создайте Product с price = 80 и quantity = 2, выведите стоимость.','160','Используйте item.price * item.quantity.'),
L('Проект: отчёт по коллекции','Сделаем функцию суммы, которая получает vector по const-ссылке. Это не копирует всю коллекцию и запрещает изменять её через параметр. Такая сигнатура сообщает намерение: функция только читает вход.\nРазделите определение функции, создание данных и вывод. Для пустого vector функция возвращает 0 благодаря начальному значению суммы.','#include <iostream>\n#include <vector>\n\nint total(const std::vector<int>& values) {\n    int result = 0;\n    for (int value : values) result += value;\n    return result;\n}\n\nint main() {\n    std::vector<int> values = {10, 20, 30};\n    std::cout << "Total: " << total(values) << "\\n";\n    return 0;\n}','const ограничивает изменение данных.|Ссылка & исключает копирование vector в параметр.|Результат возвращается как обычный int.','Не возвращайте ссылку на локальную переменную result: она исчезнет после выхода из функции.','Напишите total и выведите Total: 42 для {7, 14, 21}.','Total: 42','Сумма считается знакомым циклом; новый элемент — только форма параметра.')
];
specs.go = [
L('Пакет main и первая программа','Исходный код Go организован в пакеты. Для исполняемой программы используется пакет main и функция main. Пакет fmt предоставляет вывод. import делает его доступным в файле.\nfunc начинает объявление функции. Пока функция без параметров и результата. fmt.Println выводит аргумент и переводит строку. Имя Println начинается с заглавной буквы: так обозначено доступное из другого пакета имя.',go('fmt.Println("Hello, Go!")'),'package main определяет назначение файла.|import подключает fmt.|При запуске выполняется тело main.','Не переносите открывающую { на следующую строку: в Go это может изменить разбор кода.','Выведите Hello, LogicKernel! одной строкой.','Hello, LogicKernel!','Замените только строку в fmt.Println.'),
L('Форматирование и понятные ошибки','Go предъявляет требования к структуре файла: объявление пакета, затем импорты, затем определения. Неиспользованный импорт считается ошибкой компиляции. Это помогает не накапливать ненужные зависимости.\nКомментарии начинаются с //. Точки с запятой обычно не записывают вручную. Форматировщик gofmt приводит отступы и пробелы к единому виду. Пока важнее понимать границы main и порядок инструкций.',go('// Показываем этапы работы\nfmt.Println("Start")\nfmt.Println("Done")'),'Комментарий поясняет действия.|Оба вызова используют импортированный fmt.|Каждый Println создаёт отдельную строку вывода.','Не оставляйте import "fmt", если удалили все обращения к fmt.','Добавьте комментарий и выведите First, затем Second.','First\nSecond','Обе команды поместите внутрь main.'),
L('Переменные: var и :=','Переменную можно объявить через var score int = 10. Внутри функции короткая форма score := 10 одновременно объявляет имя и выводит тип из значения. Это не просто присваивание.\nДля изменения уже объявленной переменной используйте =. Повторное := без нового имени вызовет ошибку. Тип остаётся фиксированным: переменная с числом не превращается в строку при следующем присваивании.',go('score := 10\nscore = score + 5\nfmt.Println(score)'),'Короткое объявление выводит тип int.|Обычное присваивание обновляет значение.|Выводится 15.','Не заменяйте все = на :=: у этих операций разные задачи.','Создайте points := 40, прибавьте 10 и выведите.','50','Вторая строка использует обычный знак =.'),
L('Типы, числа и преобразования','int хранит целые числа, float64 — числа с дробной частью, string — текст, bool — логическое значение. Go не смешивает числовые типы автоматически во всех выражениях. Преобразования нужно делать явно.\nДеление двух int даёт целую часть. Для дробного результата сначала преобразуйте значения к float64. Форма float64(total) создаёт значение нового типа, не меняя исходный total.',go('total := 7\ncount := 2\naverage := float64(total) / float64(count)\nfmt.Println(average)'),'total и count имеют тип int.|Преобразование выполняется до деления.|average получает 3.5.','Нельзя складывать переменные int и float64 без согласования типов.','Посчитайте целую стоимость 5 предметов по 60 и выведите.','300','Для целого произведения подходят два int.'),
L('Строки и форматированный вывод','Строки в Go задаются двойными кавычками. Println может вывести несколько аргументов с пробелами. Printf использует шаблон: %s для строки, %d для целого числа.\nВ отличие от Println, Printf не добавляет перевод строки автоматически. Если он нужен, запишите \\n в шаблоне. Соответствие спецификаторов и аргументов делает сообщение предсказуемым.',go('name := "Ada"\npoints := 50\nfmt.Printf("%s: %d XP\\n", name, points)'),'Первый аргумент Printf — шаблон.|name подставляется вместо %s, points вместо %d.|\\n завершает сообщение новой строкой.','Не перепутайте порядок аргументов относительно спецификаторов.','Выведите Go: 80 XP, используя переменные language и points.','Go: 80 XP','Подойдёт шаблон "%s: %d XP\\n".'),
L('Условия без лишних скобок','В Go условие if записывают без обязательных круглых скобок. Оно должно быть логическим. Фигурные скобки для тела обязательны, а else располагается на одной строке с закрывающей скобкой предыдущей ветви.\nСравнение >= возвращает bool. Используйте понятные условия и отдельные ветви, прежде чем переходить к сложным комбинациям && и ||.',go('score := 80\nif score >= 70 {\n    fmt.Println("Pass")\n} else {\n    fmt.Println("Try again")\n}'),'Условие вычисляется один раз для выбора ветви.|Выводится Pass.|Ветвь else остаётся невыполненной.','Не переносите else отдельно после завершённой строки с }: автоматические ; имеют значение.','При age := 16 выведите Adult для age >= 18, иначе Young.','Young','Сохраните расположение } else { из примера.'),
L('Срезы и добавление элементов','Срез — удобное представление последовательности элементов. Запись []int{10, 20} создаёт срез целых чисел. len возвращает длину. Индексы начинаются с нуля.\nappend возвращает обновлённый срез, поэтому результат нужно сохранить. Под капотом может потребоваться новый массив; прежнее значение среза само не обязано отражать добавление.',go('scores := []int{10, 20}\nscores = append(scores, 30)\nfmt.Println(scores[0])\nfmt.Println(len(scores))'),'[]int обозначает срез целых чисел.|Результат append присваивается обратно.|Выводятся 10 и 3.','Не игнорируйте возвращаемое значение append.','Создайте []int{5, 10}, добавьте 15 и выведите длину.','3','Вызов: scores = append(scores, 15).'),
L('for и range','В Go повторение выражается через for. Форма range проходит по коллекции и может дать индекс и значение. Если индекс не нужен, используйте _: пустой идентификатор явно игнорирует его.\nСумма объявляется до цикла. На каждом шаге она увеличивается на очередной элемент. После завершения цикла Println показывает итог.',go('numbers := []int{2, 4, 6}\ntotal := 0\nfor _, number := range numbers {\n    total += number\n}\nfmt.Println(total)'),'range выдаёт пары индекс — значение.|_ отбрасывает индекс, number сохраняет значение.|Сумма после трёх шагов равна 12.','Если написать только for number := range numbers, получите индексы, а не значения.','Сложите []int{4, 8, 12} и выведите сумму.','24','Используйте два места в заголовке: _, number.'),
L('Функции и тип результата','После func идут имя и параметры. Тип результата пишется после закрывающей скобки параметров. В func double(number int) int последний int описывает возвращаемое значение.\nФункции объявляются на уровне пакета, отдельно от main. Вызов получает аргумент, выполняет тело и возвращает результат через return. Порядок определений в этом файле не требует помещать функцию до main.', 'package main\n\nimport "fmt"\n\nfunc double(number int) int {\n    return number * 2\n}\n\nfunc main() {\n    fmt.Println(double(6))\n}','number — параметр типа int.|Результат тоже int.|Вызов с 6 возвращает 12.','Не объявляйте обычную именованную функцию func double внутри main.','Напишите square(number int) int и выведите square(7).','49','Функция возвращает number * number.'),
L('map: данные по ключу','map связывает ключи и значения. Тип map[string]int означает строковые ключи и целые значения. Он удобен, например, для хранения баллов по именам.\nЧтение отсутствующего ключа возвращает нулевое значение типа. Форма value, ok := scores["Ada"] дополнительно сообщает, существовал ли ключ. Это отличает настоящие нулевые баллы от отсутствующей записи.',go('scores := map[string]int{"Ada": 40}\nscores["Ada"] = 50\nvalue, ok := scores["Ada"]\nfmt.Println(value)\nfmt.Println(ok)'),'Создаётся map с одной парой.|Присваивание обновляет значение по ключу.|Чтение возвращает 50 и true.','Не записывайте элементы в nil map: сначала создайте map через литерал или make.','Создайте map[string]int с ключом "Go" и значением 90. Выведите значение и признак наличия.','90\ntrue','Используйте value, ok при чтении.'),
L('Ошибки как возвращаемые значения','Go часто возвращает два результата: полезное значение и error. Отсутствие ошибки обозначается nil. Это заставляет явно решить, что делать при неудаче.\nstrconv.Atoi преобразует строку в int. Если строка не является допустимым числом, err не равен nil. Проверяйте ошибку до использования результата, а не после вывода.', 'package main\n\nimport (\n    "fmt"\n    "strconv"\n)\n\nfunc main() {\n    number, err := strconv.Atoi("42")\n    if err != nil {\n        fmt.Println("Invalid number")\n        return\n    }\n    fmt.Println(number)\n}','Импортируются два нужных пакета.|Atoi возвращает число и ошибку.|При ошибке return завершает main; при успехе число выводится.','Не отбрасывайте err через _, если ошибка влияет на правильность результата.','Преобразуйте "abc" через Atoi. При ошибке выведите Invalid number и завершите main.','Invalid number','Проверьте err != nil.'),
L('Проект: отчёт о результатах','Соединим срез, функцию и форматированный вывод. Функция total принимает данные и возвращает сумму, не печатая сообщения. main определяет вход и представление результата.\nДля пустого среза сумма должна быть 0. Имена должны объяснять смысл, а не повторять тип: scores яснее, чем slice1. Разделение ответственности делает маленький проект основой для более крупного.', 'package main\n\nimport "fmt"\n\nfunc total(scores []int) int {\n    result := 0\n    for _, score := range scores {\n        result += score\n    }\n    return result\n}\n\nfunc main() {\n    fmt.Printf("Total: %d\\n", total([]int{10, 20, 30}))\n}','Параметр принимает срез любой допустимой длины.|Цикл не выполняется для пустого среза.|Printf отвечает за формат сообщения.','Не делайте сумму общей переменной пакета: повторные вызовы могут влиять друг на друга.','Напишите total и выведите Total: 36 для []int{6, 12, 18}.','Total: 36','Функция должна создавать result := 0 при каждом вызове.')
];
specs.rust = [
L('Rust, компилятор и первая программа','Rust проверяет исходный код перед запуском. Компилятор сообщает о нарушениях синтаксиса, типов и правил работы с данными. Начнём с единственного действия, не пытаясь сразу изучить владение или многопоточность.\nfn объявляет функцию, main — её специальное имя для точки входа. println! — макрос вывода: знак ! является частью формы вызова. Строка в кавычках передаёт текст, а ; завершает инструкцию.',rust('println!("Hello, Rust!");'),'fn main определяет старт программы.|Тело находится между фигурными скобками.|println! выводит строку и перевод строки.','Не убирайте ! у println!: обычной функции println здесь нет.','Выведите Hello, LogicKernel! одной строкой.','Hello, LogicKernel!','Замените текст внутри println!, сохранив скобки и ;.'),
L('Читаем код и сообщения об ошибках','Код состоит из выражений и инструкций. На первых шагах каждая команда вывода заканчивается ;. Пустые строки и комментарии // нужны читателю, но не создают действий.\nЕсли компилятор указывает на символ или строку, прочитайте также пояснение и возможную подсказку. Исправляйте причину, а не меняйте случайные символы. Незакрытая скобка может повлиять на весь оставшийся файл.',rust('// Показываем этапы\nprintln!("Start");\nprintln!("Done");'),'Комментарий объясняет назначение.|Каждый println! выполняется по порядку.|Отступы показывают принадлежность к main.','Не вставляйте текст сообщения компилятора в файл программы.','Добавьте комментарий и выведите First, затем Second.','First\nSecond','Нужны две инструкции внутри main.'),
L('let и изменяемость mut','let связывает имя со значением. По умолчанию повторно присвоить другое значение нельзя. Если изменение нужно по смыслу, добавьте mut при объявлении. Так код заранее сообщает читателю, какие данные могут меняться.\nПовторное let может создать новое связывание с тем же именем — это shadowing, но на этом уроке не смешиваем его с обычным обновлением. Для счётчика используем let mut.',rust('let mut score = 10;\nscore = score + 5;\nprintln!("{}", score);'),'mut разрешает повторное присваивание.|Правая часть вычисляет новое значение 15.|{} в шаблоне заполняется следующим аргументом.','Без mut присваивание score = ... не скомпилируется.','Создайте изменяемый points = 20, прибавьте 30 и выведите.','50','Напишите let mut points = 20;.'),
L('Типы и числовые выражения','Rust имеет несколько целочисленных типов: например, i32 — знаковое 32-битное число, u32 — беззнаковое. f64 хранит число с плавающей точкой. Тип можно указать после двоеточия, а иногда компилятор выводит его сам.\nДля преобразования числовых типов в простом примере используется as. Деление целых отбрасывает дробную часть. Преобразуйте операнды до деления, если нужна дробь.',rust('let total: i32 = 7;\nlet count: i32 = 2;\nlet average = total as f64 / count as f64;\nprintln!("{}", average);'),'Аннотации фиксируют типы исходных значений.|as f64 меняет тип значения в выражении.|Результат равен 3.5.','Не предполагайте, что все типы чисел автоматически смешиваются.','Посчитайте 8 * 25 как целые числа и выведите.','200','Используйте i32 или выведение типа.'),
L('Строковые срезы и String','Литерал "Ada" имеет тип строкового среза &str: это представление текста, которым он сам не владеет. String — владеющий изменяемый текстовый буфер. Пока достаточно различать готовый текст и строку, которую нужно дополнять.\nString::from создаёт String из литерала. push_str добавляет текст и требует изменяемой переменной. Детали & и владения разберём в отдельном уроке, поэтому не нужно запоминать всё сразу.',rust('let mut name = String::from("Ada");\nname.push_str(" Lovelace");\nprintln!("{}", name);'),'String::from создаёт владеющую строку.|mut разрешает изменение содержимого.|push_str добавляет текст в конец.','Строки Rust нельзя произвольно индексировать целым числом: UTF-8 имеет переменную длину символов.','Создайте String из "Hello", добавьте ", Rust!" и выведите.','Hello, Rust!','Используйте push_str с текстом, который включает запятую и пробел.'),
L('Условия и boolean','Условие if в Rust должно иметь тип bool. Число вместо условия не подходит. Круглые скобки вокруг проверки обычно не нужны, а фигурные скобки обязательны.\nВ простом варианте выбираем одно из двух сообщений. Позже if можно использовать и как выражение, возвращающее значение; сначала важно понять выбор ветви.',rust('let score = 80;\nif score >= 70 {\n    println!("Pass");\n} else {\n    println!("Try again");\n}'),'Сравнение возвращает true.|Выполняется первый блок.|Вторая ветвь не выполняется.','Не пишите if score: Rust не превращает число в bool.','При age = 16 выведите Adult для age >= 18, иначе Young.','Young','Условие должно быть сравнением.'),
L('Массивы и векторы','Массив имеет фиксированную длину, а Vec может расти. Макрос vec![...] создаёт вектор с начальными элементами. Для добавления через push нужно объявить вектор изменяемым.\nlen возвращает количество элементов. Прямой индекс может вызвать панику, если выходит за границы. Для чтения с явной обработкой отсутствия позже используем get, возвращающий Option.',rust('let mut scores = vec![10, 20];\nscores.push(30);\nprintln!("{}", scores[0]);\nprintln!("{}", scores.len());'),'vec! создаёт Vec с элементами одного типа.|push добавляет третий элемент.|Выводятся 10 и длина 3.','scores[3] недопустим при длине 3.','Создайте vec![5, 10], добавьте 15 и выведите длину.','3','Не забудьте mut и вызов len().'),
L('Циклы, диапазоны и сумма','Цикл for может проходить по диапазону. Запись 1..4 включает 1, 2, 3, но не 4. Запись 1..=4 включает также правую границу. Это различие важно при подсчёте повторений.\nСумма меняется, поэтому ей нужен mut. Переменная цикла number получает очередное значение и используется только внутри тела.',rust('let mut total = 0;\nfor number in 1..=3 {\n    total += number;\n}\nprintln!("{}", total);'),'Диапазон даёт 1, 2, 3.|Сумма последовательно становится 1, 3, 6.|После цикла выводится 6.','1..3 не включает 3 и даст другой результат.','Посчитайте сумму чисел от 1 до 5 включительно.','15','Используйте 1..=5.'),
L('Функции и последнее выражение','Функция объявляется через fn. Тип параметра пишется после :, тип результата — после ->. Rust позволяет вернуть последнее выражение без ключевого слова return.\nТочка с запятой превращает выражение в инструкцию без возвращаемого значения. Поэтому number * 2 без ; в конце тела отдаёт число, а с ; уже не соответствует результату i32.', 'fn double(number: i32) -> i32 {\n    number * 2\n}\n\nfn main() {\n    println!("{}", double(6));\n}','Параметр имеет тип i32.|Стрелка указывает тип результата.|Последнее выражение без ; возвращает 12.','Не добавляйте ; к возвращаемому выражению, если не используете явный return.','Напишите square(number: i32) -> i32 и выведите square(7).','49','Последняя строка тела: number * number без ;.'),
L('Владение и заимствование','У значения String есть владелец, отвечающий за его время жизни. Передача строки по значению может переместить владение. После перемещения прежнее имя нельзя использовать как раньше.\nЗаимствование позволяет временно читать данные без передачи владения. Параметр &str принимает представление строки. Вызов show(&name) предоставляет доступ для чтения, а name остаётся доступным после вызова.', 'fn show(text: &str) {\n    println!("{}", text);\n}\n\nfn main() {\n    let name = String::from("Ada");\n    show(&name);\n    println!("{}", name);\n}','String принадлежит name.|&name заимствует данные для вызова; происходит преобразование к &str.|После завершения show строка всё ещё доступна в main.','Не добавляйте clone повсюду ради исчезновения ошибки: сначала поймите, нужен ли перенос или чтение.','Напишите show(text: &str), создайте String "Rust" и выведите её через show, затем снова в main.','Rust\nRust','Передайте &name в функцию.'),
L('Option, Result и явная обработка','Option описывает наличие или отсутствие значения: Some(value) либо None. Result описывает успех или ошибку: Ok(value) либо Err(error). Это разные ситуации, хотя обе требуют выбора поведения.\nМетод parse::<i32>() пытается получить число и возвращает Result. match разбирает варианты явно. Не используйте unwrap на произвольном вводе: ошибка тогда превратится в панику.',rust('let parsed = "42".parse::<i32>();\nmatch parsed {\n    Ok(number) => println!("{}", number),\n    Err(_) => println!("Invalid number"),\n}'),'parse задаёт желаемый тип i32.|Ok содержит готовое число.|Err обрабатывается сообщением; _ игнорирует детали ошибки в этом маленьком примере.','unwrap подходит не для любого ввода: сначала обдумайте неуспешный случай.','Попробуйте разобрать "abc" как i32 и при Err выведите Invalid number.','Invalid number','Используйте обе ветви match.'),
L('Проект: сумма без передачи владения','Срез &[i32] даёт функции доступ для чтения к последовательности чисел. Функция не забирает коллекцию и не изменяет её. Это удобная граница между данными проекта и вычислением.\nВ цикле for &value in values шаблон извлекает копию каждого i32 из ссылки. Числовой тип i32 допускает копирование. Результат возвращается последним выражением без ;.', 'fn total(values: &[i32]) -> i32 {\n    let mut result = 0;\n    for &value in values {\n        result += value;\n    }\n    result\n}\n\nfn main() {\n    let scores = vec![10, 20, 30];\n    println!("Total: {}", total(&scores));\n}','Функция заимствует срез.|Локальная сумма меняется, исходные данные — нет.|Пустой срез даёт 0, поскольку цикл пропускается.','Не путайте & в параметре со сменой владельца: здесь данные только заимствуются.','Напишите total и выведите Total: 27 для vec![3, 9, 15].','Total: 27','Сохраните форму &[i32] и передайте &scores.')
];
})();
