window.LKAdvanced.add('js', [
['Области видимости и замыкания','Лексическая область определяется местом объявления функции, замыкание удерживает доступ к внешним связываниям после возврата из функции, let создаёт блочную область, присваивание внешней переменной меняет состояние следующих вызовов, закрытая переменная не становится автоматически безопасной от логической гонки между асинхронными операциями, полезно возвращать узкий интерфейс вместо самого изменяемого объекта',`function counter(start = 0) {\n  let value = start;\n  return { next() { return ++value; } };\n}\nconst a = counter();\nconsole.log(a.next(), a.next());`,'реализуйте фабрику независимых счётчиков с чтением, увеличением и сбросом','два экземпляра независимы|сброс возвращает начальное значение|внутреннее состояние не доступно напрямую','неожиданно общая внешняя переменная связывает независимые экземпляры','сравните замыкание и объект с явно передаваемым состоянием'],
['Прототипы классы и this','Свойства объекта разрешаются через собственные поля и цепочку прототипов, class предоставляет синтаксис работы с этой моделью, значение this у обычной функции зависит от способа вызова, извлечение метода из объекта может потерять получателя, стрелочная функция берёт this из внешнего контекста, наследование полезно только при совместимом контракте подтипов',`class Counter {\n  constructor() { this.value = 0; }\n  next() { return ++this.value; }\n}\nconst counter = new Counter();\nconst next = counter.next.bind(counter);\nconsole.log(next());`,'постройте объект с публичным методом обратного вызова и сохранением корректного получателя','прямой вызов работает|переданный callback сохраняет состояние|экземпляры не разделяют изменяемые поля','извлечённый метод теряет this','сравните bind, стрелочное поле и передачу получателя явно'],
['Копирование и неизменяемые обновления','Оператор spread копирует только один уровень объекта, вложенные ссылки остаются общими, неизменяемое обновление создаёт новые узлы на изменённом пути и сохраняет нетронутые, это облегчает сравнение состояний и воспроизведение переходов, глубокая копия не является универсальным способом моделирования, типы значений и объём данных влияют на стоимость и семантику копирования',`const previous = { user: { name: "Ann" }, count: 1 };\nconst next = { ...previous, user: { ...previous.user, name: "Eva" } };\nconsole.log(previous.user.name, next.user.name);`,'реализуйте обновление вложенного состояния корзины без изменения предыдущего снимка','старый снимок сохраняется|обновляется только нужный товар|неизменённые ветви сохраняют ссылки','поверхностная копия не защищает вложенный объект от изменения','сравните полное копирование и структурное разделение данных'],
['Модули и публичный интерфейс','Модуль задаёт границу экспортируемых имён, импортирующий код не должен зависеть от внутренней раскладки, побочные эффекты верхнего уровня выполняются при загрузке, циклические зависимости усложняют инициализацию, разделение чистых функций и адаптеров делает модуль проверяемым, версия публичного интерфейса должна отражать изменения наблюдаемого поведения',`// pricing.mjs\nexport function total(lines) {\n  return lines.reduce((sum, line) => sum + line.price * line.count, 0);\n}\n// import { total } from './pricing.mjs';`,'спроектируйте модуль расчёта стоимости с явным экспортом и адаптером входных данных','импорт не запускает бизнес операцию|пустая корзина даёт ноль|неверное количество отвергается','побочный эффект импорта делает порядок загрузки частью скрытого контракта','сравните единый публичный вход модуля и импорт внутренних файлов'],
['Promise и порядок выполнения','Promise представляет будущий результат, обработчики выполняются через очередь микрозадач после завершения текущего синхронного стека, исключение в обработчике превращается в отклонение следующего promise, отсутствие return разрывает ожидаемую цепочку, независимые операции можно ожидать совместно, но следует явно определить реакцию на частичную неудачу',`console.log("start");\nPromise.resolve(3).then(x => x * 2).then(console.log);\nconsole.log("end");`,'реализуйте цепочку двух зависимых асинхронных преобразований и отдельную обработку отказа','порядок зависимостей соблюдён|ошибка доходит до вызывающего кода|результат возвращается из каждого шага','забытый return создаёт преждевременное завершение внешней операции','сравните последовательную цепочку и совместное ожидание независимых операций'],
['Отмена и защита от устаревшего ответа','Отмена сообщает операции, что её результат больше не нужен, но не гарантирует отмену бизнес эффекта на сервере, AbortController позволяет связать время жизни запроса с экраном, независимо от отмены полезно проверять поколение запроса перед обновлением состояния, поздний ответ предыдущего поиска не должен заменять новый, cleanup относится и к обработчикам событий и к таймерам',`let generation = 0;\nasync function latest(load, apply) {\n  const token = ++generation;\n  const result = await load();\n  if (token === generation) apply(result);\n}`,'создайте поиск с отменой предыдущей операции и защитой от поздних ответов','новый ответ не заменяется старым|уход со страницы прекращает ожидание|ошибка отмены не показывается как сбой пользователя','одного запрета повторного клика недостаточно против перестановки ответов','сравните отмену транспорта и проверку актуальности результата'],
['Итераторы генераторы и управление объёмом','Итератор выдаёт элементы по запросу, генератор упрощает хранение состояния обхода, асинхронный итератор ожидает каждый следующий элемент, потребитель определяет темп обработки, однако источник тоже должен ограничивать накопление, завершение обхода должно освобождать ресурс источника, бесконечная последовательность требует явного условия остановки',`function* range(limit) {\n  for (let i = 0; i < limit; i++) yield i;\n}\nfor (const value of range(3)) console.log(value);`,'постройте последовательный обработчик пакетного источника с ограничением числа накопленных элементов','пустой источник завершается|досрочный выход освобождает источник|порядок элементов сохраняется','ленивый интерфейс не помогает, если источник заранее загружает все данные','сравните массив результатов и поток с контролем темпа'],
['Ошибки и проверка внешних данных','Полученный JSON имеет синтаксическую структуру, но ещё не подтверждённый прикладной контракт, валидатор проверяет типы, диапазоны и обязательные поля до передачи в ядро, ошибки формата следует отличать от сетевых и бизнес отказов, проверка через truthy теряет допустимые нули и пустые строки, сообщения пользователю и технические подробности имеют разные аудитории',`function parseUser(value) {\n  if (!value || typeof value !== "object" || !Number.isInteger(value.id)) {\n    throw new TypeError("invalid user");\n  }\n  return { id: value.id };\n}\nconsole.log(parseUser({ id: 0 }));`,'создайте валидатор ответа каталога с точными проверками полей и сохранением допустимых нулей','нулевой идентификатор разрешён контрактом|массив вместо объекта отвергается|ошибка содержит путь неверного поля','проверка if value.id путает отсутствие и ноль','сравните ранний отказ и накопление списка ошибок формы'],
['DOM события и жизненный цикл','Событие имеет цель и путь распространения, делегирование позволяет обслуживать динамические элементы через общего предка, пользовательские строки безопасно отображать через textContent, повторное создание экрана не должно накапливать слушатели, жизненный цикл компонента включает не только построение DOM, но и удаление обработчиков, таймеров и ссылок на удалённые узлы',`function mount(button, output) {\n  const onClick = () => { output.textContent = "Saved"; };\n  button.addEventListener("click", onClick);\n  return () => button.removeEventListener("click", onClick);\n}`,'постройте компонент списка с делегированием событий и явным освобождением ресурсов','новые элементы работают без повторной подписки|после удаления события не меняют экран|пользовательская строка отображается как текст','повторный mount без cleanup удваивает обработчики','сравните делегирование и отдельные слушатели по времени жизни элементов'],
['Управление состоянием и переходы','Состояние описывает текущие данные, действие сообщает намерение пользователя, чистая функция перехода делает изменение воспроизводимым, побочные эффекты выполняются отдельно, явные состояния loading success error предотвращают невозможные сочетания флагов, для сложного процесса полезно перечислить допустимые переходы и определить поведение неожиданного действия',`function reduce(state, action) {\n  if (action.type === "start") return { status: "loading", data: null };\n  if (action.type === "success" && state.status === "loading") {\n    return { status: "ready", data: action.data };\n  }\n  return state;\n}`,'реализуйте автомат загрузки формы с повторной отправкой и сохранением введённых данных','успех допустим после отправки|повторный клик не создаёт второй эффект|ошибка не стирает форму','независимые boolean флаги допускают одновременный успех и ошибку','сравните reducer и распределённые присваивания по обработчикам'],
['Тестирование времени и асинхронности','Асинхронный тест обязан дождаться проверяемого результата, иначе он может завершиться до ошибки, контролируемые часы и внедрённый планировщик позволяют проверять debounce и таймаут без реального ожидания, проверки должны управлять порядком завершения запросов, а не надеяться на скорость сети, тест интерфейса наблюдает доступное поведение пользователя',`function deferred() {\n  let resolve, reject;\n  const promise = new Promise((yes, no) => { resolve = yes; reject = no; });\n  return { promise, resolve, reject };\n}\nconst pending = deferred();\npending.resolve(7);\npending.promise.then(console.log);`,'подготовьте независимые проверки поиска с управляемым порядком ответов и временем ожидания','поздний ответ воспроизводится детерминированно|отказ не теряется после завершения теста|проверки не используют реальную сеть','тест без await может быть успешным до возникновения ошибки','сравните проверку чистой логики и браузерный сценарий полного экрана'],
['HTTP кеширование и идемпотентность','HTTP статус, заголовки и тело совместно определяют контракт, кеширование требует понимания свежести и ключа, ETag позволяет проверить изменение ресурса без повторной передачи полного тела, изменяющий запрос с потерянным ответом нельзя безусловно повторять, ключ идемпотентности связывает повтор с одной операцией, локальный кеш не заменяет серверную авторизацию',`function cacheKey(method, url, userId) {\n  return JSON.stringify([method, url, userId]);\n}\nconst cache = new Map();\ncache.set(cacheKey("GET", "/courses", "u1"), { version: "v1" });\nconsole.log(cache.size);`,'спроектируйте клиент чтения с проверкой свежести и отправку команды с устойчивым ключом','разные пользователи не разделяют приватный кеш|потеря ответа не создаёт повторный эффект|устаревший ресурс запрашивается заново','кеш по одному URL может смешать данные пользователей','сравните срок свежести и условный запрос с валидатором версии'],
['Производительность и удержание памяти','Медленный интерфейс может тратить время на JavaScript, раскладку, отрисовку или ожидание ресурса, замер должен отвечать на конкретный вопрос, чередование записи и чтения геометрии может провоцировать лишнюю работу раскладки, утечка часто связана с долгоживущей ссылкой на удалённый объект, оптимизация без профиля рискует усложнить участок, который не влияет на задержку',`function summarize(values) {\n  let total = 0;\n  for (const value of values) total += value;\n  return { total, count: values.length };\n}\nconsole.log(summarize([2, 4, 6]));`,'исследуйте обработку большого списка и подготовьте измеряемое улучшение без изменения контракта','результаты до и после совпадают|замер указывает размер входа|после удаления экрана ссылки освобождаются','один быстрый замер без повторов не доказывает улучшение','сравните разбиение работы на части и уменьшение общего объёма вычислений'],
['Безопасные границы интерфейса','Недоверенные данные должны оставаться данными при отображении и передаче, HTML вставка и построение URL требуют разных правил, разрешение действия проверяется сервером независимо от скрытой кнопки, токены и секреты не включаются в логи, политика происхождения и CSP дополняют корректное построение интерфейса, но не исправляют автоматически ошибочный доступ к данным',`function safeLink(raw) {\n  const url = new URL(raw, "https://example.org");\n  if (url.protocol !== "https:") throw new Error("invalid protocol");\n  return url.href;\n}\nconsole.log(safeLink("/help"));`,'реализуйте вывод сообщений и внешних ссылок с явным набором допустимых протоколов','HTML в сообщении остаётся текстом|опасный протокол отвергается|ошибка не раскрывает секретные параметры','экранирование HTML не является проверкой безопасности URL','опишите границу ответственности клиента и серверной проверки доступа'],
['Совместимость и эволюция контракта','Клиент и сервер обновляются не одновременно, поэтому изменение поля должно учитывать старые версии, добавление необязательных данных обычно проще удаления или переосмысления существующего поля, миграция локального состояния обязана сохранять пользовательский ввод, версию протокола нужно отличать от версии содержимого, полезно проверять реальные образцы ответов обеих сторон',`function migrate(saved) {\n  if (!saved) return { version: 2, drafts: {} };\n  if (saved.version === 1) return { version: 2, drafts: saved.code || {} };\n  if (saved.version === 2) return saved;\n  throw new Error("unsupported version");\n}`,'подготовьте совместимое изменение формата черновиков и сценарий отката версии клиента','старый черновик сохраняется|повторная миграция безопасна|неизвестная версия не затирается','безусловный сброс хранилища уничтожает работу пользователя','сравните миграцию при чтении и отдельную миграцию перед запуском приложения'],
['Клиент образовательной платформы','Большое приложение следует разделять по обязанностям и направлению зависимостей, представление получает данные через транспортный интерфейс, доменные переходы отделяются от DOM, жизненный цикл запросов связан с экраном, архитектурное решение проверяется на сценариях отказа и изменения требований, покрытие контрактов важнее количества абстракций, разумная граница модуля снижает стоимость следующего изменения',`function createCatalog(api) {\n  return {\n    async load() {\n      const response = await api.list();\n      if (!Array.isArray(response.data)) throw new Error("invalid catalog");\n      return response.data;\n    }\n  };\n}`,'соберите клиент каталога, редактора и отправки решения с отделённым API, отменой, черновиками и обработкой версий','код пользователя передаётся только как текст|поздний ответ не меняет чужой экран|ошибка сети не теряет черновик','общий изменяемый объект транспорта связывает независимые экраны','обоснуйте границы модулей и способ проверки без реального бэкенда']
]);
