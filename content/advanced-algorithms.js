window.LKAdvanced.add('algorithms', [
['Инварианты и доказательство цикла','Инвариант описывает свойство до и после каждой итерации, доказательство разделяется на инициализацию, сохранение и вывод после завершения, условие завершения требует убывающей меры или ограниченного диапазона, примеры помогают обнаружить ошибку, но не заменяют рассуждение для всех допустимых входов',`function sum(values) {\n let total = 0;\n for (let i = 0; i < values.length; i++) total += values[i];\n return total;\n}\nconsole.log(sum([2,4,6]));`,'докажите корректность односканового поиска минимального значения с явным поведением пустого входа','инвариант сформулирован|пустой вход обработан|мера завершения указана','описание того что делает строка кода не является инвариантом','сравните проверку примерами и доказательство для класса входов'],
['Асимптотика и модель стоимости','Асимптотическая оценка описывает рост выбранной стоимости относительно размера входа, худший, средний и амортизированный случаи различаются, O не является точным временем, вложенные циклы не всегда дают квадрат, оценка должна учитывать реальное число итераций и операции структуры данных, эмпирический замер дополняет, а не заменяет модель',`function pairs(n) { let count=0;for(let i=0;i<n;i++)for(let j=i+1;j<n;j++)count++;return count; }\nconsole.log(pairs(5));`,'оцените несколько вариантов обработки массива и свяжите число операций с измерением','размер входа определён|учтены вложенные операции|худший случай отделён от среднего','вызов линейного поиска внутри цикла меняет общую оценку','сравните теоретический рост и константы на малых данных'],
['Рекурсия и разделяй и властвуй','Рекурсивный алгоритм решает меньшие экземпляры и объединяет результат, базовый случай должен покрывать границу, каждый вызов должен уменьшать задачу, стек вызовов является памятью алгоритма, разделение и объединение имеют собственную стоимость, рекурсивная запись не гарантирует логарифмическую глубину',`function power(x,n){if(n===0)return 1;const half=power(x,Math.floor(n/2));return n%2?half*half*x:half*half;}\nconsole.log(power(2,10));`,'реализуйте быстрое возведение в неотрицательную целую степень и докажите завершение','нулевая степень обработана|размер задачи уменьшается|числовые ограничения указаны','отрицательная степень без отдельного контракта может разрушить завершение','сравните рекурсивную и итеративную реализацию'],
['Сортировка слиянием и стабильность','Слияние объединяет две отсортированные последовательности, инвариант указывает, что построенный префикс содержит наименьшие обработанные элементы в порядке, выбор левого элемента при равенстве сохраняет стабильность, дополнительная память и число копирований входят в анализ, сортировка записей должна определять отношение порядка',`function merge(a,b){let i=0,j=0;const out=[];while(i<a.length&&j<b.length)out.push(a[i]<=b[j]?a[i++]:b[j++]);return out.concat(a.slice(i),b.slice(j));}\nconsole.log(merge([1,4],[2,3]));`,'реализуйте стабильную сортировку записей слиянием с подсчётом дополнительной памяти','равные ключи сохраняют порядок|пустые половины обработаны|компаратор согласован','выбор правой половины при равенстве может нарушить стабильность','сравните гарантированное время и стоимость памяти'],
['Бинарный поиск по монотонному условию','Бинарный поиск применим не только к поиску элемента, а к границе монотонного предиката, полуинтервал упрощает представление пустого диапазона, обновление границы должно уменьшать расстояние и сохранять инвариант, арифметика середины должна учитывать ограничения чисел, условие монотонности нужно доказать из задачи',`function lowerBound(a,x){let lo=0,hi=a.length;while(lo<hi){const mid=lo+Math.floor((hi-lo)/2);if(a[mid]<x)lo=mid+1;else hi=mid;}return lo;}\nconsole.log(lowerBound([1,3,3,7],3));`,'найдите минимальную допустимую ёмкость для монотонной задачи распределения работ','границы корректны|нет допустимого ответа обработано|монотонность обоснована','поиск по немонотонному предикату может уверенно вернуть неверный ответ','сравните поиск по элементам и по пространству ответов'],
['Хеширование и коллизии','Хеш преобразует ключ в ограниченное пространство и допускает коллизии, структура должна проверять равенство самих ключей, средняя стоимость доступа зависит от реализации и распределения, хеш не является доказательством равенства произвольных данных, для стабильного порядка нужна отдельная структура или сортировка',`function frequencies(values){const result=new Map();for(const value of values)result.set(value,(result.get(value)||0)+1);return result;}\nconsole.log([...frequencies([1,2,1])]);`,'спроектируйте индекс повторений и объясните обработку коллизий в собственной учебной таблице','разные ключи с одним хешем различаются|повторы учитываются|рост таблицы определён','сравнение только хеша может объединить разные ключи','сравните хеш таблицу и сортировку с последующим проходом'],
['Кучи и очередь приоритетов','Куча поддерживает частичный порядок между родителем и детьми, она не является полностью отсортированным массивом, восстановление свойства после вставки и удаления проходит по высоте, очередь приоритетов полезна для выбора следующего минимального элемента, равные приоритеты требуют дополнительного ключа для стабильности',`function pushHeap(heap,value){heap.push(value);let i=heap.length-1;while(i>0){const p=Math.floor((i-1)/2);if(heap[p]<=heap[i])break;[heap[p],heap[i]]=[heap[i],heap[p]];i=p;}}\nconst heap=[];[5,2,7,1].forEach(x=>pushHeap(heap,x));console.log(heap[0]);`,'реализуйте очередь задач с приоритетом, извлечением минимума и стабильностью равных ключей','свойство кучи сохраняется|пустая очередь обработана|равные приоритеты имеют порядок','линейный поиск минимума при каждом извлечении меняет стоимость','сравните кучу и отсортированный массив по соотношению вставок и извлечений'],
['Деревья поиска и балансировка','Дерево поиска связывает порядок ключей с поддеревьями, стоимость операции зависит от высоты, обычные вставки отсортированных данных могут выродить дерево в цепочку, балансировка поддерживает ограничение высоты через перестройки, диапазонный обход использует порядок, дубликаты должны иметь заранее выбранное представление',`function find(node,key){while(node){if(key===node.key)return node.value;node=key<node.key?node.left:node.right;}return undefined;}\nconsole.log(find({key:2,value:"b",left:{key:1,value:"a"}},1));`,'реализуйте учебное дерево поиска и сравните его высоту на случайном и отсортированном входе','инвариант порядка соблюдён|дубликаты имеют правило|вырожденный случай измерен','предположение о логарифмической высоте без балансировки неверно','сравните дерево, хеш индекс и отсортированный массив для диапазонных запросов'],
['Графы BFS и представление рёбер','Граф описывается вершинами и рёбрами с выбранной направленностью, список смежности удобен для разреженных графов, BFS по невзвешенным рёбрам открывает вершины слоями кратчайшего расстояния, отметка посещения должна предотвращать повторное добавление, очередь на массиве с постоянным shift может менять практическую стоимость',`function bfs(graph,start){const distance=new Map([[start,0]]),queue=[start];for(let head=0;head<queue.length;head++){const v=queue[head];for(const next of graph[v]||[]){if(distance.has(next))continue;distance.set(next,distance.get(v)+1);queue.push(next);}}return distance;}\nconsole.log([...bfs({a:["b"],b:["c"]},"a")]);`,'найдите кратчайший путь в невзвешенном графе с восстановлением последовательности вершин','недостижимая цель обработана|цикл не вызывает бесконечный обход|путь восстанавливается','BFS не решает произвольную задачу с разными весами','сравните список смежности и матрицу на разных плотностях'],
['Топологический порядок и зависимости','В ориентированном ациклическом графе существует порядок, в котором зависимости предшествуют потребителям, алгоритм по входным степеням извлекает вершины без оставшихся зависимостей, если обработаны не все вершины, есть цикл, несколько допустимых порядков требуют выбора правила детерминизма',`function ready(indegree){return Object.keys(indegree).filter(key=>indegree[key]===0).sort();}\nconsole.log(ready({build:1,fetch:0,test:1}));`,'постройте планировщик зависимых задач с обнаружением цикла и стабильным выбором доступной работы','зависимости выполняются раньше|цикл обнаруживается|несколько готовых задач упорядочены','частичный вывод порядка нельзя принимать за успешный план всего графа','сравните топологическую сортировку и рекурсивный обход с цветами'],
['Кратчайшие пути и ограничения весов','Алгоритм Дейкстры использует неотрицательность весов для фиксации наименьшего расстояния, отрицательное ребро нарушает это доказательство, устаревшие записи очереди приоритетов нужно пропускать, восстановление пути хранит предшественников, числовая модель должна представлять сумму допустимых весов без потери нужной точности',`function relax(distance,from,to,weight){if(weight<0)throw new Error("negative weight");const candidate=distance[from]+weight;if(candidate<(distance[to]??Infinity)){distance[to]=candidate;return true;}return false;}\nconst d={a:0};relax(d,"a","b",3);console.log(d.b);`,'реализуйте поиск пути с неотрицательными весами и проверкой недопустимого входа','отрицательный вес отвергается|недостижимая вершина не получает ложный путь|устаревшая запись не меняет результат','использование Дейкстры без проверки условий теряет корректность','сравните BFS, Дейкстру и алгоритм для отрицательных рёбер по ограничениям'],
['Непересекающиеся множества','DSU поддерживает объединение компонент и проверку принадлежности одной компоненте, сжатие пути и объединение по размеру уменьшают амортизированную стоимость, структура не хранит путь графа и не решает любые удаления рёбер, инвариант родителей должен исключать циклы, представители компонент не являются стабильными пользовательскими идентификаторами',`function find(parent,x){while(parent[x]!==x){parent[x]=parent[parent[x]];x=parent[x];}return x;}\nconst parent=[0,0,2];console.log(find(parent,1)===find(parent,0));`,'реализуйте DSU для добавления связей и подсчёта компонент','повторное объединение безопасно|число компонент обновляется правильно|родительские ссылки не создают цикл','представитель может измениться после объединения','сравните DSU и полный обход графа после каждого добавления'],
['Динамическое программирование','DP использует повторяющиеся подзадачи и определённую зависимость состояний, сначала задают смысл состояния и переход, затем порядок вычисления, неправильная инициализация может разрешить невозможный путь, оптимизация памяти допустима только если старые зависимости больше не нужны, восстановление решения требует дополнительной информации',`function ways(n){const dp=Array(n+1).fill(0);dp[0]=1;for(let i=1;i<=n;i++){dp[i]=dp[i-1]+(i>=2?dp[i-2]:0);}return dp[n];}\nconsole.log(ways(5));`,'решите задачу выбора с ограниченным ресурсом через DP и восстановите один оптимальный вариант','смысл состояния сформулирован|невозможное состояние различимо|ответ восстанавливается','смешение старого и нового слоя меняет тип задачи','сравните полную таблицу и сжатую память с учётом восстановления'],
['Жадный выбор и контрпримеры','Жадный алгоритм фиксирует локальное решение без возврата, корректность требует доказательства, например обменного аргумента, правдоподобный критерий не гарантирует глобального оптимума, маленький полный перебор помогает найти контрпример, свойства задачи должны явно оправдывать выбор и правило разрешения равенства',`function selectIntervals(items){const sorted=[...items].sort((a,b)=>a.end-b.end);const result=[];let end=-Infinity;for(const item of sorted){if(item.start>=end){result.push(item);end=item.end;}}return result;}\nconsole.log(selectIntervals([{start:0,end:2},{start:1,end:3},{start:2,end:4}]).length);`,'реализуйте выбор непересекающихся интервалов и докажите критерий выбора, найдите контрпример для альтернативного критерия','границы пересечения определены|жадный критерий доказан|малый перебор проверяет выбор','выбор самого короткого интервала не всегда даёт оптимальный набор','сравните жадный алгоритм и DP при изменении задачи на взвешенную'],
['Строковые алгоритмы и потоковый поиск','Поиск образца может повторять сравнения или переиспользовать информацию о совпавшем префиксе, префиксная функция позволяет откатываться к подходящему состоянию без возврата по тексту, представление символов важно для Unicode, хеширование строк допускает коллизии и требует соответствующей гарантии проверки',`function prefix(pattern){const pi=Array(pattern.length).fill(0);for(let i=1;i<pattern.length;i++){let j=pi[i-1];while(j>0&&pattern[i]!==pattern[j])j=pi[j-1];if(pattern[i]===pattern[j])j++;pi[i]=j;}return pi;}\nconsole.log(prefix("ababa"));`,'реализуйте поиск образца в потоковых фрагментах с сохранением состояния между кусками','совпадение через границу фрагментов найдено|пустой образец имеет контракт|перекрывающиеся совпадения обработаны','сброс состояния на каждом фрагменте теряет граничное совпадение','сравните KMP, прямой поиск и хеширование по гарантиям и сложности'],
['Планировщик маршрутов и зависимых работ','Итоговый алгоритмический проект начинается с модели входа и ограничений, затем выбирает структуры и доказуемые алгоритмы, правильность включает недостижимость, циклы, границы чисел и повторные данные, производительность подтверждается распределением тестов и измерениями, архитектура должна отделять модель графа от интерфейса приложения',`function validateGraph(edges){for(const edge of edges){if(!Number.isFinite(edge.weight)||edge.weight<0)throw new Error("invalid weight");}return edges.length;}\nconsole.log(validateGraph([{from:"a",to:"b",weight:3}]));`,'соберите библиотеку графовых задач с кратчайшим путём, планом зависимостей, проверкой входа и отчётом сложности','ограничения каждого алгоритма проверены|недостижимость и цикл различимы|большой вход укладывается в обоснованный бюджет','универсальный алгоритм без условий применимости даёт ложные ответы','обоснуйте выбор структур и алгоритмов по размеру, плотности и типам запросов']
]);
